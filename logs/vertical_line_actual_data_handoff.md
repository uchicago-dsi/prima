# Vertical-Line View-QC Handoff

## Current re-entry state (2026-07-12)

- Shared view QC is now target-agnostic: state labels are `present`, `absent`, and `uncertain`; target identity is required and propagated through model runs, metrics, and fallback decisions.
- Vertical morphology now exists only in `qc/targets/vertical_detector_seam_v1.txt`; use a fresh target prompt and state for every other finding.
- Full validation passed: 40 tests passed and 1 skipped. The generic gallery is live on loopback port 8767 over the existing 10-view blinded repeat manifest; progress was 0/10 at restart.
- Vertical disposition remains conservative: the 210-view confirmation found no false negatives and two false positives, including one false exhaustion. Permit same-slot target-absent fallback, but route exhausted slots to human review instead of excluding them automatically.
- Exact first check: inspect the 10-view repeat state, compare repeat labels without exposing model strata during review, then decide whether the two prior false positives are stable human/model disagreements or definition ambiguity.

- Goal: validate model-driven exact-slot L/R CC/MLO fallback before changing any production selected-view table.
- Development result: the completed 177-view fallback audit exposed 3 false negatives and 21 false positives under the original any-confidence Qwen3.5-27B policy. High-confidence-only reduced false positives to 5 but retained the 3 misses. The revised `detector_boundary_v2` prompt plus a high-confidence threshold scored TP=59/FN=0/FP=2/TN=116 and made all 125 audited slot decisions safe. This set is development data because its errors informed the prompt.
- Independent confirmation: `/scratch/annawoodard/prima_view_auto_qc/vertical_line_fallback_confirmation_full` contains 210 blinded views covering every candidate in 125 sampled slot groups. It has zero view and slot-group overlap with both prior review passes. Hidden revised-model inference job `12833719` completed `0:0` with exact 210/210 coverage; human state is fresh at 0/210.
- Impact dry run: applying the high-confidence threshold to the old full run recovered 162 previously exhausted slots and increased complete four-slot exams from 10,357 to 10,483. This is diagnostic only; production SoT remains untouched.
- Code: branch `feature/durable-dicom-lineage`; commits `6d21b4f`, `7a62d10`, `eef05e1`, `3cbcf97`, and `d278a45` remain local/unpushed. The latest commits add the revised prompt/policy/audit evaluation and standardize the gallery on port 8767 with a slightly smaller image. Preserve unrelated/user-owned `AGENTS.md` and notebook edits.
- Promotion gate: require zero unsafe slot decisions in the independent confirmation, view sensitivity at least 0.95, view specificity at least 0.90, and no repeated missed morphology. If it passes, rerun the 27B revised prompt over all 59,616 rendered candidates, build a high-confidence promotion candidate and impact report, then rerun Mirai. Do not run 397B unless the frozen 27B confirmation fails.
- Next: run `micromamba run -p /gpfs/data/huo-lab/Image/annawoodard/micromamba/envs/prima python qc/view_qc_gallery.py --manifest /scratch/annawoodard/prima_view_auto_qc/vertical_line_fallback_confirmation_full/manifest.parquet --state /scratch/annawoodard/prima_view_auto_qc/vertical_line_fallback_confirmation_full/view_qc_state.json --port 8767`, tunnel port 8767, and complete all 210 labels without opening `hidden_model_run.json`.
- First re-entry check: summarize `view_qc_state.json`; only after `remaining=0`, run `qc/evaluate_auto_qc_fallback_audit.py` with `--minimum-reject-confidence high` and inspect every unsafe or inexact group before promotion.
